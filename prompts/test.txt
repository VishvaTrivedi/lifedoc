create an authentication system where user signup using Name, age, email, password encrypt password using the bcrypt and store it in the database use proper mongodb structure for store the user data also store the type of the user by default the type is user and generate a jwt token for the verification also store this token in the session storage of the user also in the login and signup form add an checkbox for remeberme if user select the remeber me button in the form store the token in the localstorage if not then store the data/ token of the user in the session storage also make sure we have to use cyptojs for encypting and decrypt all the credentails of the user create this function to encrypt and decrypt the credentails in the utility functions also while sign up add otp based verification send a random 6 digit otp for the email make sure to use nodemailer for sending the emails to the targeted user design the user shema accordingly also create page to reset password of the user where user have to enter the registerd email and an email send to the user containing the opt using that user able to reset the password of their account  also mention all the required credentails in the env file with example make sure to create the backedn in the server folder only do no create backend in the client folder 